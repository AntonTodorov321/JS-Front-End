function addAndSubrtact(firstNum, secondNum, tirdNum) {

    let add = (firstNum, secondNum) => firstNum + secondNum;

    let subtract = (firstNum, secondNum) => firstNum - secondNum;

    let addNum = add(firstNum, secondNum)
    console.log(subtract(addNum, tirdNum));
}